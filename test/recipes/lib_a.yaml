checkoutSCM:
    scm: import
    url: ../src/lib_a/
    recipeRelative: True

buildScript: |
    cmake -S "$1" -B . \
      -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

    cmake --build . --parallel

packageScript: |
    cmake --install $1 --prefix .
